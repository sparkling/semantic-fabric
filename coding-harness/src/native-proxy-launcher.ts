// SPDX-License-Identifier: MIT

// Preserve the historical protected source/output path while the native
// filesystem boundary executes the package-independent CommonJS capability.
export { launchNativeProxy } from './native-proxy-launcher.cjs';
